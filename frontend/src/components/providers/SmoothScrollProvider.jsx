"use client";

/**
 * Smooth scroll provider — Lenis disabled to prevent click/scroll interference
 * Can be re-enabled later if needed
 */
export default function SmoothScrollProvider({ children }) {
  return <>{children}</>;
}
